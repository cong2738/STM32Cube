/*
 * sys_tic.c
 *
 *  Created on: Jun 20, 2025
 *      Author: kccistc
 */

#include "sys_tick.h"

TIM_TypeDef* hTcikTIM;

void SysTick_Init(TIM_TypeDef* TIMx){
   hTcikTIM = TIMx;
}

uint32_t getTick(){
	return hTcikTIM->CNT;
}

void SysTick_Start(){
	hTcikTIM->CR1 |= (1<<0);
}

void SysTick_Stop(){
	hTcikTIM->CR1 &= ~(1<<0);
}
