/*
 * circled_que.c
 *
 *  Created on: Jun 25, 2025
 *      Author: kccistc
 */
#include "circled_que.h"

uint8_t IsFull();
uint8_t IsEmpty();

#define QUE_SIZE 4
int front = 0, rear = 0;
int que[QUE_SIZE] = {0};

int isQueFull(Q_Type queue) {
	return front == (rear + 1) % 4;
}

int isQueEmpty(Q_Type queue) {
	return front == rear;
}

void enQue(int data) {
	if(isQueFull()) return;
	que[rear] = data;
}

void deQue() {
	if(isQueEmpty()) return;
	int res = que[front];
	front++;
	return res;
}
