/*
 * LCD.c
 *
 *  Created on: Jun 25, 2025
 *      Author: kccistc
 */

#include "LCD.h"

uint8_r lcdData = 0;

void LCD_sendData(uint8_t data){
	LCD_E_High(); // LCD_i2c enable
	// High 4bit
	lcdData = (data & 0xf0) | (lcdData & 0x0f);
	LCD_sendI2C(lcdData);
	LCD_E_Low(); // LCD_i2c disable


	LCD_E_High(); // LCD_i2c enable
	// Low 4bit
	data = data << 4;
	lcdData = (data & 0xf0) | (lcdData & 0x0f);
	LCD_sendI2C(lcdData);
	LCD_E_Low(); // LCD_i2c disable
}

LCD_sendI2C(uint4_t data) {

}
